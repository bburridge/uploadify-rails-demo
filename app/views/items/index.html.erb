<ul>
<% @items.each do |item| %>
  <li><%= link_to "#{item.title} (#{item.item_assets.count})", item %> <%= link_to "delete", item, :method => :delete, :confirm => 'Are you sure you want to delete this item and its assets?' %></li>
<% end %>
</ul>

<%= link_to "Upload", new_item_path() %>